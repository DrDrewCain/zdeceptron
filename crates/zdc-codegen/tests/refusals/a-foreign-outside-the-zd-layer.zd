# expect: this compiler emits a client bundle only
foreign hash is client
    from  "./crypto" as "hash"
    takes v is Text
    gives Text

state digest is client Text from hash with v is "a"

view
    Text digest
