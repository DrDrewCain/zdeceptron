# expect: by identity rather than by contents
record Item
    id is Text
state a is client Item starting (Item with id is "x")
state b is client Truth from cmp with a
function cmp with v
    give v is v
view
    Text "x"
