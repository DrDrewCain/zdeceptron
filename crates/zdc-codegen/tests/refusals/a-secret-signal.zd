# expect: A secret can only live where the reader is not
# The split answers this before the emitter does, as E0313. The fixture
# stays because what it pins is that `zdc check`, `zdc build` and the
# editor all say the same thing about it — not which pass said it.
secret state apiKey is client Text starting "hunter2"
view
    Text "x"
