[package]
name = "zdc-cli"
version.workspace = true
edition.workspace = true
license.workspace = true
rust-version.workspace = true
repository.workspace = true
homepage.workspace = true
description = "The ZDeceptron compiler: check, build, dev-server, deploy and language server in one binary."
# Published so `cargo install zdc-cli` works: the binary is one compiler, but
# reaching it from crates.io means every crate it links is on crates.io too.
# Path dependencies therefore carry a version as well as a path — the path is
# what a workspace build uses, the version is what a published build resolves.
# Dev-dependencies stay path-only on purpose: cargo omits a version-less
# dev-dependency from the published manifest, which is what lets the test-only
# edges between these crates form cycles the publish order does not have to
# break.

[[bin]]
name = "zdc"
path = "src/main.rs"

[dependencies]
clap.workspace = true
zdc-ast = { path = "../zdc-ast", version = "0.2.0" }
zdc-codegen = { path = "../zdc-codegen", version = "0.2.0" }
zdc-deploy = { path = "../zdc-deploy", version = "0.2.0" }
zdc-dev = { path = "../zdc-dev", version = "0.2.0" }
zdc-diagnostics = { path = "../zdc-diagnostics", version = "0.2.0" }
zdc-doc = { path = "../zdc-doc", version = "0.2.0" }
zdc-fmt = { path = "../zdc-fmt", version = "0.2.0" }
zdc-graph = { path = "../zdc-graph", version = "0.2.0" }
zdc-hir = { path = "../zdc-hir", version = "0.2.0" }
zdc-lexer = { path = "../zdc-lexer", version = "0.2.0" }
zdc-lib = { path = "../zdc-lib", version = "0.2.0" }
zdc-lsp = { path = "../zdc-lsp", version = "0.2.0" }
zdc-parser = { path = "../zdc-parser", version = "0.2.0" }
zdc-resolve = { path = "../zdc-resolve", version = "0.2.0" }
zdc-types = { path = "../zdc-types", version = "0.2.0" }

[dev-dependencies]
# `zdc lsp` is tested by speaking the protocol to it over a pipe, framing
# and all, rather than by calling the crate it wraps: what an editor
# actually depends on is the subcommand, and a test that bypassed the
# process would not notice it failing to start.
serde_json = "1"
# The SVG namespace test hands the probe page a runtime module that no
# built program under test links, because the whole point of that module
# is that only a program which draws one gets it.
zdc-runtime = { path = "../zdc-runtime" }
