[package]
name = "zdc-graph"
version.workspace = true
edition.workspace = true
license.workspace = true

[dependencies]
zdc-ast = { path = "../zdc-ast" }
zdc-hir = { path = "../zdc-hir" }
zdc-lexer = { path = "../zdc-lexer" }
zdc-types = { path = "../zdc-types" }

[dev-dependencies]
# The prelude, so that the classification of its seventeen primitives is a
# test in this crate rather than a claim in a comment (§21.9).
zdc-lib = { path = "../zdc-lib" }
zdc-parser = { path = "../zdc-parser" }
zdc-resolve = { path = "../zdc-resolve" }
