# Placement is a property of state

Three placements say *where a value lives*. A fourth, `static`, says
**when**: once, at build time.

- `client` — the browser's memory
- `server` — one invocation
- `durable` — the store
- `static` — the build
