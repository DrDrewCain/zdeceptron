# Hello, world

This file is on disk. `zdc build` reads it, renders it, and inlines the
result into the bundle as a literal — no request, no subprocess, and no
JavaScript toolchain anywhere in the path.
